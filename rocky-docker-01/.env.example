ACME_EMAIL=you@example.com

# Optional. Directory holding the secret files. Defaults to ~/.secrets.
# Set it if the secrets are elsewhere, or if compose runs under sudo (where $HOME is /root).
# SECRETS_DIR=/home/username/.secrets
